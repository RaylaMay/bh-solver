[pytest]
addopts = --strict-config --strict-markers
testpaths = tests
markers =
    native: Production Qt interaction; mandatory, never importorskip.
    worker: Real production worker/artifact integration; mandatory.
